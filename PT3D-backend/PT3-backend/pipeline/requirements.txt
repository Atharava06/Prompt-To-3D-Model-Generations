# SDXL image generation
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.30.0
safetensors>=0.4.0
Pillow>=10.0.0
datasets>=2.19.0
peft>=0.10.0

# Hunyuan3D-2 local inference
# Install from the official repo:
#   git clone https://github.com/Tencent/Hunyuan3D-2
#   cd Hunyuan3D-2 && pip install -e .
# hy3dgen is provided by that package.

# PyTorch with CUDA (install separately for the right CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
